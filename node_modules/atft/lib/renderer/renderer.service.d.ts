import { OnDestroy } from '@angular/core';
import { SceneComponent } from '../object/scene.component';
import { AbstractCamera } from '../camera/abstract-camera';
export declare class RendererService implements OnDestroy {
    private init;
    private scene;
    private camera;
    private webGlRenderer;
    private css3dRenderer;
    private aspect;
    constructor();
    ngOnDestroy(): void;
    setScene(scene: SceneComponent): void;
    setCamera(camera: AbstractCamera<any>): void;
    render(): void;
    initialize(canvas: HTMLCanvasElement): void;
    resize(canvas: HTMLCanvasElement, size: string): void;
    private calculateAspectRatio;
    private updateChildCamerasAspectRatio;
}
