import { AfterViewInit, OnChanges, SimpleChanges } from '@angular/core';
import * as THREE from 'three';
import { RendererService } from '../renderer/renderer.service';
export declare abstract class AbstractCamera<T extends THREE.Camera> implements AfterViewInit, OnChanges {
    protected rendererService: RendererService;
    camera: T;
    positionX: number;
    positionY: number;
    positionZ: number;
    zAxisUp: boolean;
    constructor(rendererService: RendererService);
    ngAfterViewInit(): void;
    protected abstract createCamera(): void;
    abstract updateAspectRatio(aspect: number): any;
    ngOnChanges(changes: SimpleChanges): void;
    protected applyPosition(): void;
    protected applyZAxisUp(): void;
}
