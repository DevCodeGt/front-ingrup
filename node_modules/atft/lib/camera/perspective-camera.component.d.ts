import { AbstractCamera } from './abstract-camera';
import * as THREE from 'three';
import { RendererService } from '../renderer/renderer.service';
export declare class PerspectiveCameraComponent extends AbstractCamera<THREE.PerspectiveCamera> {
    protected rendererService: RendererService;
    fov: number;
    near: number;
    far: number;
    constructor(rendererService: RendererService);
    protected createCamera(): void;
    updateAspectRatio(aspect: number): void;
}
