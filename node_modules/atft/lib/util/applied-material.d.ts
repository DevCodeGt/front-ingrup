import * as THREE from 'three';
/**
 * Creates material
 *
 * @param materialColor color
 * @param materialName material label as string
 * @param depthWrite enable depth write
 */
export declare function appliedMaterial(materialColor: number | THREE.Color, materialName?: string, depthWrite?: boolean): THREE.Material;
