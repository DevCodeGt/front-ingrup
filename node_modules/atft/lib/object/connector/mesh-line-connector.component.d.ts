import * as THREE from 'three';
import { AbstractConnector } from './abstract-connector';
import { AnimationService } from '../../animation/animation.service';
import { RendererService } from '../../renderer/renderer.service';
export declare class MeshLineConnectorComponent extends AbstractConnector<THREE.Mesh> {
    protected animationService: AnimationService;
    protected rendererService: RendererService;
    materialColor: number;
    transparent: boolean;
    opacity: number;
    lineWidth: number;
    depthWrite: boolean;
    depthTest: boolean;
    animated: boolean;
    animationIncrement: number;
    private geometry;
    private line;
    private lineMaterial;
    constructor(animationService: AnimationService, rendererService: RendererService);
    createConnectorObject(): THREE.Mesh;
    private animate;
    updateLineGeometry(): void;
}
