import * as THREE from 'three';
import { AbstractLazyObject3D } from '../abstract-lazy-object-3d';
import { RendererService } from '../../renderer/renderer.service';
export declare class TextMeshComponent extends AbstractLazyObject3D {
    protected rendererService: RendererService;
    material: string;
    private _materialColor;
    materialColor: number;
    private _text;
    text: string;
    size: number;
    height: number;
    curveSegments: number;
    bevelEnabled: boolean;
    bevelThickness: number;
    bevelSize: number;
    bevelOffset: number;
    bevelSegments: number;
    fontUrl: string;
    castShadow: boolean;
    receiveShadow: boolean;
    depthWrite: boolean;
    centered: boolean;
    constructor(rendererService: RendererService);
    getMaterial(): THREE.Material;
    protected loadLazyObject(): Promise<THREE.Object3D>;
}
