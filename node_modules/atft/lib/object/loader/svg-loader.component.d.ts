import { AbstractModelLoader } from './abstract-model-loader';
import * as THREE from 'three';
import { RendererService } from '../../renderer/renderer.service';
export declare class SVGLoaderComponent extends AbstractModelLoader {
    protected rendererService: RendererService;
    overrideMaterialColor: number;
    private _overrideMaterialColor;
    material: string;
    depthWrite: boolean;
    maxX: number;
    maxY: number;
    centered: boolean;
    private loader;
    constructor(rendererService: RendererService);
    protected loadLazyObject(): Promise<THREE.Object3D>;
}
